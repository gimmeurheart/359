import initAnother from './parts/another';
import initSliders from './parts/sliders';
import initForm from './parts/form';

$(document).ready(() => {
  initAnother();
  initSliders();
  initForm();
});
